server:
  port: 0

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Ps1h0/shop-ms
          default-label: main
          username:
          password:
          clone-on-start: true
